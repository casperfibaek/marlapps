Fix width of resume/pause button in soundscape
Check for updates openes new line, which bounces the interface. Make sure it does not bounce.
app manifest should not have order? That is already defined in the registry apps.json

Update mirror app
Update todo app

Remove legacy code etc, do a clean version 1.0 app
